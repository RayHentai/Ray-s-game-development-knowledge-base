# 🔢232. 用栈实现队列

**所属模块**：[[🏠算法知识库主页]]
**题目链接**：[232. 用栈实现队列 - 力扣（LeetCode）](https://leetcode.cn/problems/implement-queue-using-stacks/description/)
**难度**：⬛ Easy / ⬜ Medium / ⬜ Hard
**关联**：
**查阅次数**：0

---

## 题目描述

请你仅使用两个栈实现先入先出队列。队列应当支持一般队列支持的所有操作（`push`、`pop`、`peek`、`empty`）：

实现 `MyQueue` 类：

- `void push(int x)` 将元素 x 推到队列的末尾
- `int pop()` 从队列的开头移除并返回元素
- `int peek()` 返回队列开头的元素
- `boolean empty()` 如果队列为空，返回 `true` ；否则，返回 `false`

**说明：**

- 你 **只能** 使用标准的栈操作 —— 也就是只有 `push to top`, `peek/pop from top`, `size`, 和 `is empty` 操作是合法的。
- 你所使用的语言也许不支持栈。你可以使用 list 或者 deque（双端队列）来模拟一个栈，只要是标准的栈操作即可。

**示例 1：**

**输入：**
\["MyQueue", "push", "push", "peek", "pop", "empty"]
\[\[], \[1], \[2], \[], \[], \[]]
**输出：**
\[null, null, null, 1, 1, false]

**解释：**
MyQueue myQueue = new MyQueue();
myQueue.push(1); // queue is: \[1]
myQueue.push(2); // queue is: \[1, 2] (leftmost is front of the queue)
myQueue.peek(); // return 1
myQueue.pop(); // return 1, queue is \[2]
myQueue.empty(); // return false

---

## 第一反应

> 看到题目时第一个想到的思路（不管对不对，先记下来）

---

## 解法一：模拟

**思路：用一个Stack临时存储压入的数据，如果要出队，就用pop临时Stack，用另一个Stack存储倒腾完之后pop或者peek即可**

**时间复杂度：** O(n)、Peek、Empty为O(1)
**空间复杂度：** O(n)

```csharp
public class MyQueue {
    Stack<int> stack;
    Stack<int> stackRe;
    public MyQueue() {
        stack = new Stack<int>();
        stackRe = new Stack<int>();
    }
    
    public void Push(int x) {
        stack.Push(x);
    }
    
    public int Pop() {
        MoveToOutput();
        return stackRe.Pop();
    }
    
    public int Peek(){
        MoveToOutput();
        return stackRe.Peek();
    }
    public bool Empty() => stack.Count == 0 && stackRe.Count == 0;
    private void MoveToOutput(){
        if (stackRe.Count == 0){
            foreach(int item in stack) 
                stackRe.Push(item);
            stack.Clear();
        }
    }
}
```

---

## 解法二：[解法名称]（最优）

**核心思路：**

**时间复杂度：** O()
**空间复杂度：** O()

```csharp
// 代码
```

---

## 解法三：[解法名称]（可选）

**核心思路：**

**时间复杂度：** O()
**空间复杂度：** O()

```csharp
// 代码
```

---

## 关联题目

-
-

---

## 我的踩坑记录

-

---

*最后更新：*
