# 🔢541. 反转字符串 II

**所属模块**：[[🏠算法知识库主页]]
**题目链接**：[541. 反转字符串 II - 力扣（LeetCode）](https://leetcode.cn/problems/reverse-string-ii/)
**难度**：⬛ Easy / ⬜ Medium / ⬜ Hard
**关联**：
**查阅次数**：0

---

## 题目描述

给定一个字符串 `s` 和一个整数 `k`，从字符串开头算起，每计数至 `2k` 个字符，就反转这 `2k` 字符中的前 `k` 个字符，再重新计数。

- 如果剩余字符少于 `k` 个，则将剩余字符全部反转。
- 如果剩余字符小于 `2k` 但大于或等于 `k` 个，则反转前 `k` 个字符，其余字符保持原样。

**示例 1：**
**输入：** s = "abcdefg", k = 2
**输出：**"bacdfeg"

**示例 2：**
**输入：** s = "abcd", k = 2
**输出：**"bacd"

---

## 第一反应

> 看到题目时第一个想到的思路（不管对不对，先记下来）

---

## 解法一：模拟

**思路：以 `2k` 为步长遍历，找准每次反转的起点和终点，**

**时间复杂度：** O(n)
**空间复杂度：** O(n)

```csharp
public class Solution {
    public string ReverseStr(string s, int k) {
        char[] c = s.ToCharArray();
        int left = 0;
        int right = 0;
        int step = 2 * k;
        for (int i = 0; i < s.Length; i += step){
            left = i;
            right = Math.Min(i + k - 1, s.Length - 1);
            while(left < right){
                (c[left], c[right]) = (c[right], c[left]);
                ++left;
                --right;
            }
        }
        return new String(c);
    }
}
```

---

## 解法二：现代语法基础上的模拟（最优）

**核心思路：用 `Span` 处理字符串，用 `..` 范围语法判断反转范围，用 `Reverse` 反转字符**

**时间复杂度：** O(n)
**空间复杂度：** O(1)

```csharp
public class Solution {
    public string ReverseStr(string s, int k) {
        Span<char> span = s.ToCharArray().AsSpan();
        for (int i = 0; i < s.Length; i += k * 2)
            span[i + k < s.Length ? i..(i + k) : i..].Reverse();
        return span.ToString();
    }
}
```

---

## 解法三：[解法名称]（可选）

**核心思路：**

**时间复杂度：** O()
**空间复杂度：** O()

```csharp
// 代码
```

---

## 关联题目

-
-

---

## 我的踩坑记录

-

---

*最后更新：*
