# 🔢24.两两交换链表中的节点

**所属模块**：[[🏠算法知识库主页]]
**题目链接**：[24. 两两交换链表中的节点 - 力扣（LeetCode）](https://leetcode.cn/problems/swap-nodes-in-pairs/description/)
**难度**：⬜ Easy / ⬛ Medium / ⬜ Hard
**关联**：
**查阅次数**：0

---

## 题目描述

给你一个链表，两两交换其中相邻的节点，并返回交换后链表的头节点。你必须在不修改节点内部的值的情况下完成本题（即，只能进行节点交换）。

**示例 1：**

![[两两交换链表中的节点 图示.png|261]]

**输入：** head = \[1,2,3,4]
**输出：**\[2,1,4,3]

**示例 2：**

**输入：** head = \[]
**输出：** \[]

**示例 3：**

**输入：** head = \[1]
**输出：**\[1]

---

## 第一反应

模拟指针移动的过程

---

## 解法一：虚拟指针（模拟）（最优）

**核心思路：**

**时间复杂度：** O(n)
**空间复杂度：** O(1)

```csharp
public class Solution {
    public ListNode SwapPairs(ListNode head) {
        ListNode dummy = new (0, head);
        ListNode cur = dummy;
        while (cur.next != null && cur.next.next != null){
            ListNode nowCur = cur.next; // 1 
            ListNode nextCur = nowCur.next.next;//3
            cur.next = cur.next.next;// dummy -> 2 
            cur = cur.next;//cur指向2
            cur.next = nowCur; // dummy -> 2 -> 1 ->
            cur = cur.next;//cur指向1
            cur.next = nextCur;//cur next 3
        }
        return dummy.next;
    }
}
```

---

## 解法三：[解法名称]（可选）

**核心思路：**

**时间复杂度：** O()
**空间复杂度：** O()

```csharp
// 代码
```

---

## 关联题目

-
-

---

## 我的踩坑记录

-

---

*最后更新：*
