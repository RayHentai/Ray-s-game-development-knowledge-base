# 📝 08 脚本基础规则

**所属模块**：[[00 Unity入门阶段总览]]
**关联**：[[06 工作原理与反射机制]]
**查阅次数**：0

---

## 脚本创建规则

- 不在VS中创建脚本了 统一在Unity中创建
- 可以放在 `Assets` 文件夹下的任何位置（建议同意文件夹管理）
- 创建的脚本默认都继承 `MonoBehavior`，继承了它才能够**挂载在 GameObject 上**
- 脚本名与类名必须一致（不一致会报错）
- 建议不要用中文命名，建议用帕斯卡命名法
- 没有特殊需求，不用管命名空间

---

## 继承 MonoBehavior 的类

- 继承 `MonoBehavior` 的类**才能**挂载到 GameObject 上
- 继承了 `MonoBehavior` 的脚本**不能**使用 `new` 关键字实例化（Unity 有自己的实例化机制）
- 继承了 `MonoBehavior` 的脚本**不要**写构造函数（没有意义，Unity 不通过构造函数创建）
- 继承了 `MonoBehavior` 的脚本可以在一个对象上挂多个（如果没有加 `DisallowMultipleComponent` 特性
- 继承 `MonoBehavior` 的类也可以再次被继承 遵循面向对象继承多态的规则

---

## 不继承 MonoBehavior 的类

> 不能挂载在 GameObject 上，但对于我们依然有重要作用。

**主要用途：**

```csharp
// 用途1：数据结构类，用于存储信息
public class PlayerInfo
{
    public string name;
    public bool sex;
    public int atk, def, hp;
}

// 用途2：单例模式的管理器类
public class MonsterMgr
{
    private static MonsterMgr instance = new MonsterMgr();
    public static MonsterMgr Instance => instance;
}
```

---

## 其他脚本相关内容

---

### 脚本执行的先后顺序

| 时间越小越先执行 | [[脚本 Execution.png]] |
| -------- | -------------------- |

---

### 脚本默认内容位置

| Unity版本\Editor\Data\Resources\ScriptTemplates | [[脚本默认内容位置.png]] |
| --------------------------------------------- | ---------------- |

---

## 注意事项

- 所有继承 MonoBehavior 的脚本，最终都会挂载到 GameObject 游戏对象上
- **拖拽到 GameObject 对象后，再改变脚本中变量默认值，界面上不会改变**（已经序列化的值不会跟着代码改）

---

## 设置编程工具

> 在 Unity Edit → Preferences → External Tools 中设置脚本编辑器（推荐 Visual Studio 或 VS Code）。

---

## 练习

> 1. 为什么继承 MonoBehavior 的类不能写构造函数？


- 因为继承了 MonoBehavior 的类不能 new，只能挂载。
- 所以就不会通过构造函数去实例化对象，写构造函数没有意义。


> 2. 为什么继承 MonoBehavior 的类不能直接 new 出来用，要挂载？


- 这是 Unity 定下的规则
- 继承 MonoBehavior 的类只能挂载在 GameObject 上配合使用


> 3. 请写一个类用于说明不继承 MonoBehavior 的类对于我们的作用

```csharp
// ① 数据结构类，用于存储信息
public class PlayerInfo
{
    public string name;
    public bool sex;
    public int atk, def, hp;
}

// ② 单例模式的管理器类
public class MonsterMgr
{
    private static MonsterMgr instance = new MonsterMgr();
    public static MonsterMgr Instance => instance;
}
```

---

## 我的踩坑记录

- 脚本名和类名不一致会导致挂载失败

---

*最后更新：*
