# 📑 08 索引器

**所属模块**：[[00 CSharp 核心阶段总览]]
**关联**：[[07 成员属性]]
**查阅次数**：1

---

## 核心理解

- 可以以中括号的形式访问自定义类中的元素，规则自己定，访问时和数组一样
- 适合在类中有数组的情况下使用，可以方便访问和进行逻辑处理

---

## 语法

**注意事项：**
- `get` 和 `set` 的用法同成员属性（可以只有一个，可以加访问修饰符）
- 索引器也可以重载（支持不同类型的下标，如 `this[string key]`）
- 结构体里面也可以写索引器

```csharp
//语法
// 访问修饰符 返回值 this[参数类型 参数名, ...]
// {
//     内部的写法和规则同成员属性相同
//     get{}
//     set{}
// }

class Person
{
	private string name;
	private int age;
	private Person[] fridens;
	
	public Peison this[int index]
	{
		get
		{
			return fridens[index];
		}
		set
		{
			friend[index] = value;//value代表外部赋的值
		}
	}
}

Person p = new Person();
p[0] = new Person();

//get 和 set内可写逻辑 确保不会报错
get
{
	if (friends == null || friends.Length - 1 < index)
		return null;
	return friends[index];
}
set
{
	if (friends == null)
		friends = new Person[](){ value };
	else if (friends.Length - 1 < index)
		friends[friends.Length - 1] = value;//自定义规则，把最后一个元素顶掉 
	friends[index] = value;
}
```

---
## 练习

> 1. 自定义一个整形数组类，该类中有一个整形数组变量，为它封装增删查改的方法

```csharp
class Arr
{
    private int[] arr;
    private int length;
    private int capacity;

    public Arr()
    {
        length = 0;
        capacity = 5;
        arr = new int[capacity];
    }

    public void Add(int value)
    {
        if (length < capacity)
        {
            arr[length] = value;
            ++length;
        }
        else
        {
            capacity *= 2;
            int[] tempArr = new int[capacity];
            for (int i = 0; i < length; i++) 
	            tempArr[i] = arr[i];
            arr = tempArr;
            tempArr[length] = value;
            ++length;
        }
    }

    public void Remove(int value)
    {
        for (int i = 0; i < length; i++)
        {
            if (arr[i] == value) 
            { 
	            RemoveAt(i); 
	            return; 
            }
        }
        Console.WriteLine("没有找到");
    }

    public void RemoveAt(int index)
    {
        if (index > length - 1)    
        { 
	        Console.WriteLine("越界！"); 
	        return; 
        }
        if (index == length - 1)   
        { 
	        arr[index] = 0; 
	        --length; 
        }
        else
        {
            for (int i = index; i < length - 1; i++) 
	            arr[i] = arr[i + 1];
            --length;
        }
    }

    // 查改：索引器
    public int this[int index]
    {
        get 
        { 
        return 
	        arr[index]; 
        }
        set 
        { 
	        arr[index] = value; 
        }
    }
}

Arr arr = new Arr();
arr.Add(1000); arr.Add(2000); arr.Add(3000); arr.Add(4000); arr.Add(5000);
arr.RemoveAt(4);
Console.WriteLine(arr[0]);
```

---

## 我的踩坑记录

- 索引器不加边界检查，越界直接报异常

---

*最后更新：*
