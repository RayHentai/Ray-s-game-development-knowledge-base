# 🛡️ 异常捕获

**所属模块**：[[00 CSharp 入门阶段总览]]
**关联**：[[09 类型转换]]

**查阅次数**：0

---

## 什么是异常捕获

> 程序运行时出错（比如 Parse 到非法字符串）会直接崩溃。
> 用 try-catch 可以"兜住"错误，让程序继续运行。

---

## 基本语法

```csharp
try
{
    // 把可能报错的代码放这里
    int num = int.Parse(Console.ReadLine());
}
catch
{
    // 报错后执行这里
    Console.WriteLine("输入有误，请输入数字！");
}
finally
{
    // 不管有没有出错，最后都会执行（可选）
    // 常用于收尾工作：关闭文件、释放资源
}
```

---

## 获取具体错误信息

```csharp
catch (Exception e)
{
    // e.Message 包含具体错误描述，方便调试跟踪
    Console.WriteLine("发生错误：" + e.Message);
}
```

---

## 练习

> 1. 请用户输入一个数字，如果输入有误，则提示用户输入错误

```csharp
try
{
    Console.WriteLine("请输入一个数字");
    int i = int.Parse(Console.ReadLine());
}
catch
{
    Console.WriteLine("输入错误，请重新输入");
}
```

> 2. 提示用户输入姓名、语文、数学、英语成绩  如果输入的成绩有误，则提示用户输入错误，否则将输入的字符串转为整形变量存储

```csharp
// 方法1：一步到位（任一出错则全部重来）
try
{
    Console.WriteLine("请输入姓名"); Console.ReadLine();
    Console.WriteLine("请输入语文成绩");
    int yuWen  = int.Parse(Console.ReadLine());
    Console.WriteLine("请输入数学成绩");
    int shuXue = int.Parse(Console.ReadLine());
    Console.WriteLine("请输入英语成绩");
    int yingYu = int.Parse(Console.ReadLine());
}
catch { Console.WriteLine("输入错误，请重新输入"); }
```

```csharp
// 方法2：分开捕获（报错不影响继续输入其他成绩）
Console.WriteLine("请输入姓名"); Console.ReadLine();

try
{
    Console.WriteLine("请输入语文成绩");
    int yuWen = int.Parse(Console.ReadLine());
}
catch { Console.WriteLine("语文成绩输入不合法"); }

try
{
    Console.WriteLine("请输入数学成绩");
    int shuXue = int.Parse(Console.ReadLine());
}
catch { Console.WriteLine("数学成绩输入不合法"); }

try
{
    Console.WriteLine("请输入英语成绩");
    int yingYu = int.Parse(Console.ReadLine());
}
catch { Console.WriteLine("英语成绩输入不合法"); }
```

---

## 我的踩坑记录

- 

---

*最后更新：*
