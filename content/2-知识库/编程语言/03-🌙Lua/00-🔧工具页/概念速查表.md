# 🔗 概念速查表

> 快速定位某个概念属于哪个知识点，直接跳转。

---

## 变量与类型

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| 8种数据类型：nil / number / string / boolean / function / table / userdata / thread | [[02 变量]] |
| `type()` 获取变量类型，返回值是 string | [[02 变量]] |
| 所有变量无需声明类型，自动推断（类似 C# 的 var） | [[02 变量]] |
| 使用未声明变量不报错，默认值为 nil | [[02 变量]] |
| `''` 和 `""` 都是字符串，没有 char 类型 | [[02 变量]] |
| `local` 局部变量，只在当前脚本/语句块中有效 | [[09 多Lua脚本执行]] |
| `_G` 全局表，所有全局变量以键值对存储于此 | [[09 多Lua脚本执行]] |
| 多变量赋值 `a, b, c = 1, 2, 3`，多退少补 nil | [[10 特殊用法]] |

---

## 字符串操作

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| `#str` 获取字符串长度（中文占3，英文占1） | [[03 字符串操作]] |
| `..` 字符串拼接 | [[03 字符串操作]] |
| `string.format()` 格式化字符串，`%d` `%s` `%a` | [[03 字符串操作]] |
| `tostring()` 转换为字符串 | [[03 字符串操作]] |
| `[[多行内容]]` 多行字符串 | [[03 字符串操作]] |
| `string.upper()` / `string.lower()` 大小写转换 | [[03 字符串操作]] |
| `string.find()` 查找子串，返回起止索引 | [[03 字符串操作]] |
| `string.sub()` 截取字符串（索引从1开始） | [[03 字符串操作]] |
| `string.rep()` 字符串重复 | [[03 字符串操作]] |
| `string.gsub()` 字符串替换，返回新串和替换次数 | [[03 字符串操作]] |
| `string.byte()` 字符转 ASCII 码 | [[03 字符串操作]] |
| `string.char()` ASCII 码转字符 | [[03 字符串操作]] |

---

## 运算符

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| `~=` 不等于（不是 `!=`） | [[04 运算符]] |
| `^` 幂运算（不是异或） | [[04 运算符]] |
| `and` / `or` / `not` 逻辑运算符 | [[04 运算符]] |
| 短路规则（and / or） | [[04 运算符]] |
| 字符串可参与算术运算，自动转 number | [[04 运算符]] |
| 不支持自增减 `++`、复合赋值 `+=` | [[04 运算符]] |
| 不支持位运算符、不支持三目运算符 | [[04 运算符]] |
| `and`/`or` 返回值是操作数之一，可模拟三目 `条件 and x or y` | [[10 特殊用法]] |

---

## 控制流

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| `if 条件 then ... end` 单分支 | [[05 条件分支语句]] |
| `elseif`（不能写成 `else if`，中间不能有空格） | [[05 条件分支语句]] |
| `while 条件 do ... end` | [[06 循环语句]] |
| `repeat ... until 条件` —— 类似 do-while，条件为**结束条件** | [[06 循环语句]] |
| `for i=1,n do ... end` 数值 for，默认步进 +1 | [[06 循环语句]] |
| `for i=1,n,step do` 自定义步进，负数为倒序 | [[06 循环语句]] |
| `break` 跳出循环 | [[06 循环语句]] |

---

## 函数

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| `function 名() end` / `变量 = function() end` 两种声明方式 | [[07 函数]] |
| 函数必须先声明后调用（从上到下执行） | [[07 函数]] |
| 参数个数不匹配不报错，多了丢弃，少了补 nil | [[07 函数]] |
| 多返回值 `return a, b, c`，接收时多退少补 nil | [[07 函数]] |
| `function Func(...)` 变长参数，用 `{...}` 转为表 | [[07 函数]] |
| 不支持函数重载，同名函数后者覆盖前者 | [[07 函数]] |
| 函数嵌套、返回内部函数 | [[07 函数]] |
| 闭包：内部函数捕获外部变量，延长其生命周期 | [[07 函数]] |

---

## 表（Table）

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| 表是一切复杂数据的基础：数组、字典、类 | [[08 复杂数据类型 表]] |
| 数组索引从 **1** 开始（不是0） | [[08 复杂数据类型 表]] |
| `#表` 获取表长度（表中有 nil 会导致结果不准确） | [[08 复杂数据类型 表]] |
| 自定义索引（包括负数、0、跳跃索引）对长度计算的影响 | [[08 复杂数据类型 表]] |
| `ipairs` 顺序迭代，遇 nil 停止，忽略非正整数键 | [[08 复杂数据类型 表]] |
| `pairs` 全量迭代，遍历所有键值对（含字符串键） | [[08 复杂数据类型 表]] |
| 表模拟字典：字符串键，`[]` 或 `.` 访问 | [[08 复杂数据类型 表]] |
| `.` vs `:` 调用方法的区别（`:` 自动传入调用者作为第一参数） | [[08 复杂数据类型 表]] |
| `table.insert()` 插入元素 | [[08 复杂数据类型 表]] |
| `table.remove()` 删除元素（默认最后一个，可指定索引） | [[08 复杂数据类型 表]] |
| `table.sort()` 排序，默认升序，可传入比较函数 | [[08 复杂数据类型 表]] |
| `table.concat()` 拼接表中字符串元素 | [[08 复杂数据类型 表]] |

---

## 多脚本执行

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| `require("脚本名")` 加载并执行脚本（同一脚本只执行一次） | [[09 多Lua脚本执行]] |
| 脚本末尾 `return 变量` 向外导出内容 | [[09 多Lua脚本执行]] |
| `package.loaded["脚本名"]` 检查脚本是否已加载 | [[09 多Lua脚本执行]] |
| `package.loaded["脚本名"] = nil` 卸载脚本（使其可重新执行） | [[09 多Lua脚本执行]] |

---

## 协程

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| `coroutine.create(函数)` 创建协程，返回 thread 对象 | [[11 协程]] |
| `coroutine.wrap(函数)` 创建协程，返回可直接调用的函数 | [[11 协程]] |
| `coroutine.resume(协程)` 启动或恢复协程，返回是否成功+yield返回值 | [[11 协程]] |
| `coroutine.yield(返回值)` 挂起协程，值传回给 resume | [[11 协程]] |
| `coroutine.status(协程)` 获取状态：dead / suspended / running | [[11 协程]] |
| `coroutine.running()` 获取当前正在运行的协程 | [[11 协程]] |

---

## 元表

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| `setmetatable(子表, 元表)` 设置元表 | [[12 元表]] |
| `getmetatable(表)` 获取元表 | [[12 元表]] |
| `rawget(t, k)` 绕开 `__index`，直接读取自身 | [[12 元表]] |
| `rawset(t, k, v)` 绕开 `__newindex`，直接写入自身 | [[12 元表]] |
| `__index`：访问不存在的键时触发，支持套娃 | [[12 元表]] |
| `__newindex`：向不存在的键赋值时触发 | [[12 元表]] |
| `__tostring`：`print(表)` 时触发 | [[12 元表]] |
| `__call`：将表当函数调用时触发 | [[12 元表]] |
| 运算符重载：`__add` `__sub` `__mul` `__div` `__eq` `__lt` `__le` `__concat` 等 | [[12 元表]] |

---

## 面向对象

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| 封装：表 + `new()` 方法 + `__index = self` + `setmetatable` | [[13 面向对象]] |
| 继承：`subClass()` 方法通过 `_G` 创建子类表并设置元表 | [[13 面向对象]] |
| 多态：子类中直接重写同名方法 | [[13 面向对象]] |
| `self.base` 指向父类，调用父类方法用 `self.base.方法(self)`（不能用 `:`） | [[13 面向对象]] |
| `self` 代表冒号调用时传入的第一个参数（调用者） | [[13 面向对象]] |

---

## 标准库

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| `os.time()` 获取当前系统时间戳 | [[14 自带库]] |
| `os.time({year, month, day})` 指定时间转时间戳 | [[14 自带库]] |
| `os.date("*t")` 获取详细时间表（含 year/month/day/hour 等） | [[14 自带库]] |
| `math.abs / floor / ceil / max / min / sqrt / pow` | [[14 自带库]] |
| `math.deg(弧度)` 弧度转角度 | [[14 自带库]] |
| `math.cos / sin` 三角函数（传入弧度） | [[14 自带库]] |
| `math.modf()` 整数小数分离，返回两个值 | [[14 自带库]] |
| `math.random()` 随机数 | [[14 自带库]] |
| `math.randomseed(os.time())` 设置随机种子 | [[14 自带库]] |
| `package.path` 脚本搜索路径 | [[14 自带库]] |

---

## 垃圾回收

| 概念 / 关键词 | 所在文件 |
| --- | --- |
| `collectgarbage("count")` 获取当前内存占用（KB） | [[15 Lua GC]] |
| `collectgarbage("collect")` 手动触发完整 GC | [[15 Lua GC]] |
| 变量置 nil 解除引用，对象变为可回收垃圾 | [[15 Lua GC]] |
| Unity 热更新开发中尽量不用自动 GC | [[15 Lua GC]] |
