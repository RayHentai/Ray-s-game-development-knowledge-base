# 07 UI管理模块

**所属模块**：[[00 程序基础框架 总览]]
**关联**：[[00 UGUI 总览]] | [[15 里氏替换原则与is as]] | [[08 Dictionary]] | [[13 事件]] | [[11 MonoBehavior 重要内容]]
**查阅次数**：0

---

## Panel 基类

---

### 基本原理

**传统的UI制作流程**
1. 拼面板（必须做）
2. 声明组件（重复工作）
3. 查找组件（重复工作）
4. 监听事件（重复工作）
5. 处理逻辑（必须做）

**流程：**
1. 制作UI面板基类，帮助我们自动化的查找组件，监听事件，无需每次写大量冗余代码
	- 将2、3、4步做成自动化的，无需重复去做，解决方案一般有两种：
		- 自动化工具生成代码（Unity编辑器开发之编辑器拓展中）
		- 基类中规范冗余代码（我们将讲解的）

2. 制作UI管理器，管理所有UI面板，UI面板的显示隐藏都通过UI管理器来进行管理
	  - 提供公共API供外部使用比如：
		  - 显示面板
		  - 隐藏面板
		  - 获取面板
		  - 添加自定义事件
		  - 等等

按照这个思路制作UI管理模块后
之后在制作UI功能时，只需要把重点放在拼面板，和面板逻辑处理上了

---

### 基本实现

---

**主要实现思路：**
1. 在基类中完成声明组件、查找组件、监听组件相关功能
2. 让子类可以直接处理事件逻辑，获取指定控件

**主要实现内容：**
1. 通用的查找组件功能
2. 通用的添加事件功能
3. 显示面板、隐藏面板时的逻辑执行虚函数
4. 获取指定组件的功能
等等

**关键点：**
制定控件命名规则
1. 要使用的组件需要改名
2. 不使用只用于显示的组件可以使用默认名

```csharp
public abstract class BasePanel : MonoBehaviour
{
    public Dictionary<string, UIBehaviour> controlDic = new Dictionary<string, UIBehaviour>();//用于存储控件的容器
    private static List<string> defaultNameList = new List<string>() //存储使用默认名控件的容器 默认名表示无需处理
    {
        "Image",
        "Text (TMP)",
        "RawImage",
        "Background",
        "Checkmark",
        "Label",
        "Text (Legacy)",
        "Arrow",
        "Placeholder",
        "Fill",
        "Handle",
        "Viewport",
        "Scrollbar Horizontal",
        "Scrollbar Vertical"
    };
    protected virtual void Awake()
    {
        //记录常用控件
        //基础控件放在后面 因为组合控件中也会存在基础控件的脚本 避免容器键同名报错
        FindChildControl<Button>();
        FindChildControl<Toggle>();
        FindChildControl<Slider>();
        FindChildControl<InputField>();
        FindChildControl<ScrollRect>();
        FindChildControl<Dropdown>();
        FindChildControl<Text>();
        FindChildControl<TextMeshPro>();
        FindChildControl<Image>();
    }
    /// <summary>
    /// 获取控件
    /// </summary>
    /// <typeparam name="T">控件类型</typeparam>
    /// <param name="name">控件名</param>
    /// <returns></returns>
    public T GetControl<T>(string name) where T : UIBehaviour
    {
        if (controlDic.ContainsKey(name))
        {
            T control = controlDic[name] as T;
            if (control == null)
                Debug.LogError($"不存在对应名字为{name}类型为{typeof(T)}的组件");
            return control;
        }
        else
        {
            Debug.LogError($"不存在对应名字为{name}的组件");
            return null;
        }
    }
    /// <summary>
    /// 显示控件
    /// </summary>
    public abstract void ShowMe();
    /// <summary>
    /// 隐藏控件
    /// </summary>
    public abstract void HideMe();
    /// <summary>
    /// 按钮点击监听函数
    /// </summary>
    /// <param name="btnName">按钮名</param>
    protected virtual void ClickBtn(string btnName)
    {
        
    }
    /// <summary>
    /// 单选框值改变监听函数
    /// </summary>
    /// <param name="btnName">控件名</param>
    /// <param name="Value">当前值</param>
    protected virtual void ToggleValueChange(string btnName, bool Value)
    {

    }
    /// <summary>
    /// 滑动条值改变监听函数
    /// </summary>
    /// <param name="btnName">控件名</param>
    /// <param name="Value">当前值</param>
    protected virtual void SliderValueChange(string btnName, float Value)
    {

    }
    /// <summary>
    /// 寻找所有控件并记录
    /// </summary>
    /// <typeparam name="T">控件类型</typeparam>
    private void FindChildControl<T>() where T : UIBehaviour
    {
        T[] controls = GetComponentsInChildren<T>(true);
        for (int i = 0; i < controls.Length; i++) 
        {
            string controlName = controls[i].gameObject.name; //闭包 得到当前控件名

            if (!controlDic.ContainsKey(controlName) && !defaultNameList.Contains(controlName))//如果没有记录该控件 并且不是默认名
            {
                controlDic.Add(controlName, controls[i]); //得到控件 根据不同控件处理不同逻辑
                if (controls[i] is Button)
                {
                    (controls[i] as Button).onClick.AddListener(() => 
                    {
                        ClickBtn(controlName);
                    });
                }
                else if (controls[i] is Toggle)
                {
                    (controls[i] as Toggle).onValueChanged.AddListener((value) =>
                    {
                        ToggleValueChange(controlName, value);
                    });
                }
                else if (controls[i] is Slider)
                {
                    (controls[i] as Slider).onValueChanged.AddListener((value) =>
                    {
                        SliderValueChange(controlName, value);
                    });
                }
                else if (controls[i] is InputField)
                {
                    (controls[i] as InputField).onValueChanged.AddListener((value) =>
                    {

                    });
                }
                else if (controls[i] is ScrollRect)
                {
                    (controls[i] as ScrollRect).onValueChanged.AddListener((value) =>
                    {

                    });
                }
                else if (controls[i] is Dropdown)
                {
                    (controls[i] as Dropdown).onValueChanged.AddListener((value) =>
                    {

                    });
                }
            }
        }
    }
}
```

```csharp title:测试
public class TestPanel : BasePanel
{
    private void Start()
    {
        print($"测试，获取到的按钮控件名为：{GetControl<Button>("BtnSetting").gameObject.name}");
    }
    protected override void ClickBtn(string btnName)
    {
        switch (btnName)
        {
            case "BtnStart":
                print("测试，点击开始按钮");
                break;
            case "BtnSetting":
                print("测试，点击设置按钮");
                break;
            case "BtnQuit":
                print("测试，点击退出按钮");
                break;
        }
    }
    public override void HideMe() {  }
    public override void ShowMe() {  }
}
```

---

## UIMgr

**UI管理器的作用：方便管理所有UI面板**
- 控制UI面板的显示隐藏
- 提供公共API供外部使用，比如：
	- 显示面板
	- 隐藏面板
	- 获取面板
	- 添加自定义事件
	- 等等

---

### 基本原理

**主要思路：**
1. UI面板在任何场景都会显示，因此Canvas和EventSystem对象应该过场景不移除，并且保证唯一性和动态创建
	- UI管理器为不继承MonoBehaviour的单例模式
	- 在构造函数中动态创建设置好的Canvas和EventSystem预设体(如果使用了UI摄像机，也需要单独处理摄像机预设体)

2. UI面板的显示可以存在层级（前后）关系，我们可以预先创建好层级对象，提供获取层级对象的方法
	- 在Canvas下创建好管理层级的子对象，之后面板作为对应层级对象的子对象达到分层作用
	- 提供获取层级对象的方法

3. 提供方法
	- 存储面板的容器
	- 显示面板
	- 隐藏面板
	- 获取面板

---

### 基本实现

```csharp
/// <summary>
/// UI层枚举
/// </summary>
public enum E_UILayer
{
    /// <summary>
    /// UI底层
    /// </summary>
    Bottom,
    /// <summary>
    /// UI中层
    /// </summary>
    Middle,
    /// <summary>
    /// UI上层
    /// </summary>
    top,
    /// <summary>
    /// UI系统层
    /// </summary>
    System
}
/// <summary>
/// UI管理器
/// 规则：面板类名要和面板预设体名一致！！！
/// </summary>
public class UIMgr : BaseManager<UIMgr>
{
    private Camera uiCamera; //ui摄像机
    private Canvas uiCanvas; //ui根对象
    private EventSystem uiEventSystem; //ui输入系统
    private Transform bottomLayer; //UI底层
    private Transform middleLayer; //UI中层
    private Transform topLayer; //UI上层
    private Transform systemLayer; //UI系统层
    private Dictionary<string, BasePanel> panelDic = new Dictionary<string, BasePanel>(); //存储面板的容器
    private UIMgr()
    {
        uiCamera = GameObject.Instantiate(ResMgr.Instance.Load<GameObject>("UI/UICamera").GetComponent<Camera>());
        GameObject.DontDestroyOnLoad(uiCamera); //实例化UI摄像机 并设置过场景不移除
        uiCanvas = GameObject.Instantiate(ResMgr.Instance.Load<GameObject>("UI/Canvas").GetComponent<Canvas>());
        uiCanvas.worldCamera = uiCamera; //设置Canvas上的摄像机
        GameObject.DontDestroyOnLoad(uiCanvas);//实例化Canvas 并设置过场景不移除 
        uiEventSystem = GameObject.Instantiate(ResMgr.Instance.Load<GameObject>("UI/EventSystem").GetComponent<EventSystem>());
        GameObject.DontDestroyOnLoad(uiEventSystem);//实例化EventSystem 并设置过场景不移除 
        bottomLayer = uiCanvas.transform.Find("Bottom"); //找到UI底层
        middleLayer = uiCanvas.transform.Find("Middle"); //找到UI中层
        topLayer = uiCanvas.transform.Find("Top"); //找到UI上层
        systemLayer = uiCanvas.transform.Find("System"); //找到UI系统层
    }
    /// <summary>
    /// 得到UI层级父对象
    /// </summary>
    /// <param name="layer">UI层级</param>
    /// <returns></returns>
    public Transform GetLayerFather(E_UILayer layer)
    {
        switch (layer)
        {
            case E_UILayer.Bottom:
                return bottomLayer;
            case E_UILayer.Middle:
                return middleLayer;
            case E_UILayer.top:
                return topLayer;
            case E_UILayer.System:
                return systemLayer;
            default:
                Debug.LogError("没有找到对应的层级，请检查或者手动添加层级枚举");
                return null;
        }
    }
    /// <summary>
    /// 显示面板
    /// 注意：面板类名要和面板预设体名一致！！！
    /// </summary>
    /// <typeparam name="T">面板类型</typeparam>
    /// <param name="layer">面板层级 默认为中间层</param>
    /// <param name="callback">回调函数 异步加载的形式可以通过回调函数传回面板 默认null</param>
    /// <param name="isSync">是否同步加载 默认false</param>
    public void ShowPanel<T>(E_UILayer layer = E_UILayer.Middle, UnityAction<T> callback = null, bool isSync = false) where T : BasePanel
    {
        string panelName = typeof(T).Name;
        if (panelDic.ContainsKey(panelName)) //如果存在面板
        {
            panelDic[panelName].ShowMe(); //显示面板
            callback?.Invoke(panelDic[panelName] as T); //直接调用回调函数传出去使用
            return;
        }
        //不存在面板 
        ABResMgr.Instance.LoadResAsync<GameObject>("ui", panelName, (res) =>
        {
            Transform father = GetLayerFather(layer); //层级的处理 获取层级父对象
            if (father == null) //如果没有获取 默认为中层
                father = middleLayer;
            GameObject panelObj = GameObject.Instantiate(res, father, false);//实例化 设置父对象
            T panel = panelObj.GetComponent<T>(); //得到组件
            panel.ShowMe(); //显示组件
            callback?.Invoke(panel); //调用记录的回调函数 将面板传出去
            //存储
            panelDic.Add(panelName, panel);
        }, isSync);
    }
    /// <summary>
    /// 移除面板
    /// </summary>
    /// <typeparam name="T">面板类型</typeparam>
    public void HidePanel<T>() where T : BasePanel
    {
        string panelName = typeof(T).Name;
        if (panelDic.ContainsKey(panelName))
        {
            panelDic[panelName].HideMe();//隐藏面板
            GameObject.Destroy(panelDic[panelName].gameObject);//移除面板
            panelDic.Remove(panelName);//从字典中删除
        }
    }
    /// <summary>
    /// 得到面板
    /// </summary>
    /// <typeparam name="T">面板类型</typeparam>
    /// <returns></returns>
    public T GetPanel<T>() where T : BasePanel
    {
        string panelName = typeof(T).Name;
        if (panelDic.ContainsKey(panelName))
            return panelDic[panelName] as T;
        return null;
    }
}

```

```csharp title:测试
public class Main : MonoBehaviour
{
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.S))
        {
            UIMgr.Instance.ShowPanel<TestPanel>(E_UILayer.System, null, true);
        }
        if (Input.GetKeyDown(KeyCode.H))
        {
            UIMgr.Instance.HidePanel<TestPanel>();
        }
    }
}
```

**潜在问题：**
1.如果同一帧连续显示同一个面板，应该如何处理？
2.如果不想要直接销毁面板，应该如何处理？
3.如果想要为控件添加一些非默认事件应该处理？

---

### 优化

---

#### 异步加载问题

之前制作UI管理器时，加载资源时是在测试模式下
始终使用的是编辑器同步加载模式
若真正使用异步加载时，可能会存在报错风险

举例重现问题：
1. 构建AB包
2. 采用异步加载方式 加载AB包中的UI面板资源 
3. 同一帧显示两次UI面板 同一帧会执行两次异步加载 面板显示两个 字典会存两次 并报错
4. 同一帧显示又隐藏UI面板  异步加载还没执行完毕就会执行隐藏的逻辑 会直接找不到字典中的面板 等待加载完毕之后又会显示面板 

**主要优化思路：**
1. 造成问题的关键点
	- 由于异步加载 字典容器中没有及时存储将要显示的面板对象
	- 我们需要在显示面板时 一开始就存储面板的相关信息
	- 这样不管是二次显示还是隐藏，都能够知道是否已经在加载面板了

2.分情况考虑问题（异步加载中 和 异步加载结束）
- 显示相关
	1. 若加载中想要显示，应该记录回调，加载结束后统一调用
	2. 若加载结束后想显示，直接显示
- 隐藏相关
	1. 若加载中想要隐藏，应该改变标识
	2. 若加载结束想要隐藏，直接隐藏
	3. 若压根没有，不用处理
- 获取相关
	1. 若加载中想要获取，应该等待加载结束后再处理获取逻辑
	2. 若加载结束想要获取，直接获取
	3. 若压根没有，不用处理

**主要解决的问题：**
1. 同一帧连续显示同一面板，避免重复进行异步加载后回调重复往字典中添加面板数据
2. 同一帧 显示——>隐藏——>显示 同一面板问题，面板能够正常显示
3. 获取面板时如果正在加载中，等加载结束后再获取处理逻辑

```csharp
/// <summary>
/// UI层枚举
/// </summary>
public enum E_UILayer
{
    /// <summary>
    /// UI底层
    /// </summary>
    Bottom,
    /// <summary>
    /// UI中层
    /// </summary>
    Middle,
    /// <summary>
    /// UI上层
    /// </summary>
    top,
    /// <summary>
    /// UI系统层
    /// </summary>
    System
}
/// <summary>
/// UI管理器
/// 规则：面板类名要和面板预设体名一致！！！
/// </summary>
public class UIMgr : BaseManager<UIMgr>
{
    /// <summary>
    /// 主要用于里氏替换原则 在字典中用父类容器装载子类对象
    /// </summary>
    private abstract class BasePanelInfo { }
    /// <summary>
    /// 用于存储面板信息 和加载完成的回调函数
    /// </summary>
    /// <typeparam name="T"></typeparam>
    private class PanelInfo<T> : BasePanelInfo where T : BasePanel
    {
        public T panel;
        public UnityAction<T> callback;
        public bool isHide;
        public PanelInfo(UnityAction<T> callback)
        {
            this.callback += callback;
        }
    }
    private Camera uiCamera; //ui摄像机
    private Canvas uiCanvas; //ui根对象
    private EventSystem uiEventSystem; //ui输入系统
    private Transform bottomLayer; //UI底层
    private Transform middleLayer; //UI中层
    private Transform topLayer; //UI上层
    private Transform systemLayer; //UI系统层
    private Dictionary<string, BasePanelInfo> panelDic = new Dictionary<string, BasePanelInfo>(); //存储面板的容器
    private UIMgr()
    {
        uiCamera = GameObject.Instantiate(ResMgr.Instance.Load<GameObject>("UI/UICamera").GetComponent<Camera>());
        GameObject.DontDestroyOnLoad(uiCamera); //实例化UI摄像机 并设置过场景不移除
        uiCanvas = GameObject.Instantiate(ResMgr.Instance.Load<GameObject>("UI/Canvas").GetComponent<Canvas>());
        uiCanvas.worldCamera = uiCamera; //设置Canvas上的摄像机
        GameObject.DontDestroyOnLoad(uiCanvas);//实例化Canvas 并设置过场景不移除 
        uiEventSystem = GameObject.Instantiate(ResMgr.Instance.Load<GameObject>("UI/EventSystem").GetComponent<EventSystem>());
        GameObject.DontDestroyOnLoad(uiEventSystem);//实例化EventSystem 并设置过场景不移除 
        bottomLayer = uiCanvas.transform.Find("Bottom"); //找到UI底层
        middleLayer = uiCanvas.transform.Find("Middle"); //找到UI中层
        topLayer = uiCanvas.transform.Find("Top"); //找到UI上层
        systemLayer = uiCanvas.transform.Find("System"); //找到UI系统层
    }
    /// <summary>
    /// 得到UI层级父对象
    /// </summary>
    /// <param name="layer">UI层级</param>
    /// <returns></returns>
    public Transform GetLayerFather(E_UILayer layer)
    {
        switch (layer)
        {
            case E_UILayer.Bottom:
                return bottomLayer;
            case E_UILayer.Middle:
                return middleLayer;
            case E_UILayer.top:
                return topLayer;
            case E_UILayer.System:
                return systemLayer;
            default:
                Debug.LogError("没有找到对应的层级，请检查或者手动添加层级枚举");
                return null;
        }
    }
    /// <summary>
    /// 显示面板
    /// 注意：面板类名要和面板预设体名一致！！！
    /// </summary>
    /// <typeparam name="T">面板类型</typeparam>
    /// <param name="layer">面板层级 默认为中间层</param>
    /// <param name="callback">回调函数 异步加载的形式可以通过回调函数传回面板 默认null</param>
    /// <param name="isSync">是否同步加载 默认false</param>
    public void ShowPanel<T>(E_UILayer layer = E_UILayer.Middle, UnityAction<T> callback = null, bool isSync = false) where T : BasePanel
    {
        string panelName = typeof(T).Name;
        if (panelDic.ContainsKey(panelName)) //如果存在面板
        {
            PanelInfo<T> panelInfo = panelDic[panelName] as PanelInfo<T>; //取出字典中占好位的信息
            if (panelInfo.panel == null) //正在异步加载中
            {
                panelInfo.isHide = false; //如果在移除前再次执行显示的方法 则改变是否移除的标识
                if (callback != null)
                    panelInfo.callback += callback; //记录回调函数
            }
            else //已经加载结束
            {
                panelInfo.panel.ShowMe(); //显示面板
                callback?.Invoke(panelInfo.panel); //直接调用回调函数传出去使用
            }
            return;
        }
        //不存在面板 
        panelDic.Add(panelName, new PanelInfo<T>(callback)); //先存入字典进行占位 
        ABResMgr.Instance.LoadResAsync<GameObject>("ui", panelName, (res) =>
        {
            PanelInfo<T> panelInfo = panelDic[panelName] as PanelInfo<T>; //取出字典中占好位的信息
            if (panelInfo.isHide) //如果需要移除
            {
                GameObject.Destroy(panelInfo.panel); //移除
                panelDic.Remove(panelName);//删除字典记录
                return;
            }
            Transform father = GetLayerFather(layer); //层级的处理 获取层级父对象
            if (father == null) //如果没有获取 默认为中层
                father = middleLayer;
            GameObject panelObj = GameObject.Instantiate(res, father, false);//实例化 设置父对象
            T panel = panelObj.GetComponent<T>(); //得到组件
            panel.ShowMe(); //显示组件
            panelInfo.callback?.Invoke(panel); //调用记录的回调函数 将面板传出去
            panelInfo.callback = null; //回调执行完置空 避免内存泄漏
            //存储
            panelInfo.panel = panel;
        }, isSync);
    }
    /// <summary>
    /// 移除面板
    /// </summary>
    /// <typeparam name="T">面板类型</typeparam>
    public void HidePanel<T>() where T : BasePanel
    {
        string panelName = typeof(T).Name;
        if (panelDic.ContainsKey(panelName))
        {
            PanelInfo<T> panelInfo = panelDic[panelName] as PanelInfo<T>; //取出字典中占好位的信息
            if (panelInfo.panel == null) //如果正在异步加载
            {
                panelInfo.isHide = true; //改变标识 需要移除
                panelInfo.callback = null; //清空叠加的回调函数
            }
            else //如果已经异步加载完毕
            {
                panelInfo.panel.HideMe();//隐藏面板
                GameObject.Destroy(panelInfo.panel.gameObject);//移除面板
                panelDic.Remove(panelName);//从字典中删除
            }
        }
    }
    /// <summary>
    /// 得到面板
    /// </summary>
    /// <typeparam name="T">面板类型</typeparam>
    /// <returns></returns>
    public T GetPanel<T>() where T : BasePanel
    {
        string panelName = typeof(T).Name;
        if (panelDic.ContainsKey(panelName))
            return panelDic[panelName] as T;
        return null;
    }
}
```


```csharp title:测试
public class Main : MonoBehaviour
{
    void Start()
    {
        ABResMgr.Instance.LoadResAsync<GameObject>("model", "Cube", (obj) => { Instantiate(obj); });
        UIMgr.Instance.ShowPanel<TestPanel>(E_UILayer.System, (obj) =>
        {
            print("第一次调用显示面板");
        });
        UIMgr.Instance.HidePanel<TestPanel>();
        UIMgr.Instance.ShowPanel<TestPanel>(E_UILayer.System, (obj) =>
        {
            print("移除后又调用显示面板");
        });
        UIMgr.Instance.ShowPanel<TestPanel>(E_UILayer.System, (obj) =>
        {
            print("第二次调用显示面板");
        });
        UIMgr.Instance.ShowPanel<TestPanel>(E_UILayer.System, (obj) =>
        {
            print("第三次调用显示面板");
        });
    }
```

---

#### 隐藏面板可选销毁

目前隐藏面板时，会直接将面板销毁，下次创建时再重新创建

**优点：**
1. 当存在内存压力时
2. 直接销毁面板后，当内存不足时会触发GC
3. 不会因为存在没有使用的面板引用而造成内存崩溃

**缺点：**
1. 会产生内存垃圾加快GC的触发
2. 频繁的销毁创建会增加性能消耗

也就是说不能直接将面板隐藏改成不销毁
而应该改为**可以自己控制最好**
可以根据项目的实际情况 选择性的使用失活或销毁

**主要制作思路：**
1. 无需使用缓存池，因为缓存池主要是提供给非唯一对象使用的
2. UI面板大部分情况下是唯一的，因此我们直接在UI管理器中修改逻辑即可

**主要实现内容：**
1. 隐藏面板时，可以选择销毁还是失活
2. 显示面板时，如果存在直接激活，如果不存在再重新创建

```csharp
/// <summary>
/// UI层枚举
/// </summary>
public enum E_UILayer
{
    /// <summary>
    /// UI底层
    /// </summary>
    Bottom,
    /// <summary>
    /// UI中层
    /// </summary>
    Middle,
    /// <summary>
    /// UI上层
    /// </summary>
    top,
    /// <summary>
    /// UI系统层
    /// </summary>
    System
}
/// <summary>
/// UI管理器
/// 规则：面板类名要和面板预设体名一致！！！
/// </summary>
public class UIMgr : BaseManager<UIMgr>
{
    /// <summary>
    /// 主要用于里氏替换原则 在字典中用父类容器装载子类对象
    /// </summary>
    private abstract class BasePanelInfo { }
    /// <summary>
    /// 用于存储面板信息 和加载完成的回调函数
    /// </summary>
    /// <typeparam name="T"></typeparam>
    private class PanelInfo<T> : BasePanelInfo where T : BasePanel
    {
        public T panel;
        public UnityAction<T> callback;
        public bool isHide;
        public PanelInfo(UnityAction<T> callback)
        {
            this.callback += callback;
        }
    }
    private Camera uiCamera; //ui摄像机
    private Canvas uiCanvas; //ui根对象
    private EventSystem uiEventSystem; //ui输入系统
    private Transform bottomLayer; //UI底层
    private Transform middleLayer; //UI中层
    private Transform topLayer; //UI上层
    private Transform systemLayer; //UI系统层
    private Dictionary<string, BasePanelInfo> panelDic = new Dictionary<string, BasePanelInfo>(); //存储面板的容器
    private UIMgr()
    {
        uiCamera = GameObject.Instantiate(ResMgr.Instance.Load<GameObject>("UI/UICamera").GetComponent<Camera>());
        GameObject.DontDestroyOnLoad(uiCamera); //实例化UI摄像机 并设置过场景不移除
        uiCanvas = GameObject.Instantiate(ResMgr.Instance.Load<GameObject>("UI/Canvas").GetComponent<Canvas>());
        uiCanvas.worldCamera = uiCamera; //设置Canvas上的摄像机
        GameObject.DontDestroyOnLoad(uiCanvas);//实例化Canvas 并设置过场景不移除 
        uiEventSystem = GameObject.Instantiate(ResMgr.Instance.Load<GameObject>("UI/EventSystem").GetComponent<EventSystem>());
        GameObject.DontDestroyOnLoad(uiEventSystem);//实例化EventSystem 并设置过场景不移除 
        bottomLayer = uiCanvas.transform.Find("Bottom"); //找到UI底层
        middleLayer = uiCanvas.transform.Find("Middle"); //找到UI中层
        topLayer = uiCanvas.transform.Find("Top"); //找到UI上层
        systemLayer = uiCanvas.transform.Find("System"); //找到UI系统层
    }
    /// <summary>
    /// 得到UI层级父对象
    /// </summary>
    /// <param name="layer">UI层级</param>
    /// <returns></returns>
    public Transform GetLayerFather(E_UILayer layer)
    {
        switch (layer)
        {
            case E_UILayer.Bottom:
                return bottomLayer;
            case E_UILayer.Middle:
                return middleLayer;
            case E_UILayer.top:
                return topLayer;
            case E_UILayer.System:
                return systemLayer;
            default:
                Debug.LogError("没有找到对应的层级，请检查或者手动添加层级枚举");
                return null;
        }
    }
    /// <summary>
    /// 显示面板
    /// 注意：面板类名要和面板预设体名一致！！！
    /// </summary>
    /// <typeparam name="T">面板类型</typeparam>
    /// <param name="layer">面板层级 默认为中间层</param>
    /// <param name="callback">回调函数 异步加载的形式可以通过回调函数传回面板 默认null</param>
    /// <param name="isSync">是否同步加载 默认false</param>
    public void ShowPanel<T>(E_UILayer layer = E_UILayer.Middle, UnityAction<T> callback = null, bool isSync = false) where T : BasePanel
    {
        string panelName = typeof(T).Name;
        if (panelDic.ContainsKey(panelName)) //如果存在面板
        {
            PanelInfo<T> panelInfo = panelDic[panelName] as PanelInfo<T>; //取出字典中占好位的信息
            if (panelInfo.panel == null) //正在异步加载中
            {
                panelInfo.isHide = false; //如果在移除前再次执行显示的方法 则改变是否移除的标识
                if (callback != null)
                    panelInfo.callback += callback; //记录回调函数
            }
            else //已经加载结束
            {
                if (!panelInfo.panel.gameObject.activeSelf) //如果当前是失活状态
                    panelInfo.panel.gameObject.SetActive(true);//激活
                panelInfo.panel.ShowMe(); //显示面板
                callback?.Invoke(panelInfo.panel); //直接调用回调函数传出去使用
            }
            return;
        }
        //不存在面板 
        panelDic.Add(panelName, new PanelInfo<T>(callback)); //先存入字典进行占位 
        ABResMgr.Instance.LoadResAsync<GameObject>("ui", panelName, (res) =>
        {
            PanelInfo<T> panelInfo = panelDic[panelName] as PanelInfo<T>; //取出字典中占好位的信息
            if (panelInfo.isHide) //如果需要移除
            {
                GameObject.Destroy(panelInfo.panel); //移除
                panelDic.Remove(panelName);//删除字典记录
                return;
            }
            Transform father = GetLayerFather(layer); //层级的处理 获取层级父对象
            if (father == null) //如果没有获取 默认为中层
                father = middleLayer;
            GameObject panelObj = GameObject.Instantiate(res, father, false);//实例化 设置父对象
            T panel = panelObj.GetComponent<T>(); //得到组件
            panel.ShowMe(); //显示组件
            panelInfo.callback?.Invoke(panel); //调用记录的回调函数 将面板传出去
            panelInfo.callback = null; //回调执行完置空 避免内存泄漏
            //存储
            panelInfo.panel = panel;
        }, isSync);
    }
    /// <summary>
    /// 移除面板
    /// </summary>
    /// <typeparam name="T">面板类型</typeparam>
    public void HidePanel<T>(bool isDestroy = false) where T : BasePanel
    {
        string panelName = typeof(T).Name;
        if (panelDic.ContainsKey(panelName))
        {
            PanelInfo<T> panelInfo = panelDic[panelName] as PanelInfo<T>; //取出字典中占好位的信息
            if (panelInfo.panel == null) //如果正在异步加载
            {
                panelInfo.isHide = true; //改变标识 需要移除
                panelInfo.callback = null; //清空叠加的回调函数
            }
            else //如果已经异步加载完毕
            {
                if (isDestroy) //如果要移除
                {
                    panelInfo.panel.HideMe();//隐藏面板
                    GameObject.Destroy(panelInfo.panel.gameObject);//移除面板
                    panelDic.Remove(panelName);//从字典中删除
                }
                else //如果不移除则失活
                    panelInfo.panel.gameObject.SetActive(false);
            }
        }
    }
    /// <summary>
    /// 得到面板
    /// </summary>
    /// <typeparam name="T">面板类型</typeparam>
    /// <returns></returns>
    public T GetPanel<T>() where T : BasePanel
    {
        string panelName = typeof(T).Name;
        if (panelDic.ContainsKey(panelName))
            return panelDic[panelName] as T;
        return null;
    }
}

```

```csharp title:测试
public class Main : MonoBehaviour
{
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.S))
        {
            UIMgr.Instance.ShowPanel<TestPanel>(E_UILayer.System, null, true);
        }
        if (Input.GetKeyDown(KeyCode.H))
        {
            UIMgr.Instance.HidePanel<TestPanel>();
        }
    }
}
```

---

#### 自定义事件添加函数

**在制作UI功能时，经常会有这样的需求:**
1. 为一些不带默认事件的控件添加自定义事件，比如Image、Text这些基础组件，想为他们添加点击、单击、拖拽等事件监听
2. 为一些带默认事件的控件添加自定义事件，比如为 Button 按钮添加鼠标进入、鼠标移除等事件监听
3. 等等

因此完全可以把添加自定义事件封装为一个公共函数，方便为各种控件添加自定义事件

```csharp
    /// <summary>
    /// 添加自定义触发器事件
    /// </summary>
    /// <param name="control">控件</param>
    /// <param name="type">事件类型</param>
    /// <param name="callback">响应函数</param>
    public static void AddCustomEventListener(UIBehaviour control, EventTriggerType type, UnityAction<BaseEventData> callback)
    {
        EventTrigger trigger = control.gameObject.AddComponent<EventTrigger>();
        if (trigger == null) //保证对象只挂载一个事件触发器组件
            trigger = control.gameObject.AddComponent<EventTrigger>();
        EventTrigger.Entry entry = new EventTrigger.Entry();
        entry.eventID = type;
        entry.callback.AddListener(callback);
        trigger.triggers.Add(entry);
    }
```

---

## 什么时候用

> 适用场景，帮助建立使用直觉

- 场景一：
- 场景二：
- 反例（不该用的时候）：

---

## 注意事项 / 易错点

> ⚠️ 把容易踩的坑提前列出来

- 
- 

---

## 我的踩坑记录

> ⭐ 这里最有价值！把自己犯过的错误写下来，写上日期

- （日期）踩坑描述 → 解决方法

---

## 延伸阅读

> 这个知识点延伸出去的方向，学完后可以探索

- [[]]相关知识点
- 

---

*最后更新：*
