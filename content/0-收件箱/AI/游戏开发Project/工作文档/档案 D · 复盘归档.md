# 档案 D · 复盘归档

## 档案 D · 场景01 · 首次全域摸底(理解层+重现层闭卷)

**场景**:人生第一次闭卷检验。理解层口头摸底(小车核心)+ 重现层闭卷(从零写圆柱隧道 mesh)。

**实际反应**:
①理解层——多数概念初答带一个错位(顶点空间/triangles存啥/绕序现象/sharedMesh),但一经追问即自恢复或诚实认不知;架构层(无限拼接)主动讲出、完整。
②重现层——顶点生成顺(自恢复快);triangles 卡很久,尤其"四角偏移右下vs左上"绕五轮,反复用"换一组写死数字""起点不同""顺逆时针"回避正面确认偏移公式,最终靠"看z坐标"钉死才通。
③自主动作——绕序自己看效果反、闭合自己确认、countX/countZ手误自己会在提示后改。

**诊断(卡在理解链条哪环)**:卡在 **"符号-空间映射"环**——不是不懂原理(sin/cos会推、三角形拼法会),是"数组索引 ↔ 空间位置"的对应在脑中无稳定模型,triangles 偏移全靠现推,故易错、易在追问下动摇。理解层"结构在标签松"(一戳回),重现层"边界精确性"最薄(fencepost 3次、偏移5轮)。

**目标反应**:偏移公式应能凭"同圈+1/跨圈+每圈点数"的空间含义直接写出,不需五轮试错;fencepost 应形成"桩vs段"条件反射。

**启发**:
①摸底证实根源1判断准确(架构≠复刻,缺口在细节层不在理解端)。
②新增精确定位:重现缺口不是笼统"不会复刻",是特指 **"索引/坐标边界精确性"** 这一薄弱层——下阶段练习应针对性打这个,不需重练顶点/架构。
③回避模式在闭卷中有新表现:卡壳时倾向"换个能跑的具体解"绕过"正面确认抽象公式",需被拦住逼其面对。

**来源**:实战 · 全域摸底会话

**档案D · 场景01 附注 · 本次摸底覆盖边界**:实际只覆盖档一中小车项目相关机制(mesh/绕序/sin-cos/无限拼接)+闭卷。档一通用基础未测(C#语言特性/Unity通用/UI,清单见开头)。理解层地图仅代表小车机制,非档一全貌。**待办:C#/Unity通用基础单开一轮理解层摸底。**

## 档案 D · 场景 02 · 第二轮通用基础摸底

> 本条为进行中记录,二轮收口时并入完整场景 02。

**知识点:委托与事件 —— 理解层判定:结构在,一戳全恢复。**

- 初答自评"基本忘了",实际:委托定义准(存方法的类型 / `.Invoke()` / `?.Invoke()` 空安全)、Action/Func 轴一提就接住(返回值为轴,泛型上限 16/16 记得牢)、event 外部限制结论**完全正确**(外部只能 `+=`/`-=`,不能 `=`、不能 Invoke;委托无此限制)、内存泄漏机制能自主推出(订阅列表持有引用 → GC 判定有引用 → 回收不掉)。
- 模式与一轮一致:非塌方,是**生疏 + 边界细节松**。松的具体点:①`=` vs `+=`(在字段式事件内部其实可 `=`,详见下);②裸调 null 委托会 NRE(已会 `?.Invoke()`,属习惯非缺口)。
- 净赚细节:经纠错后掌握"字段式事件在声明类内部 = 私有委托字段本身,故内部可 `=`/可调用"。
- 术语补充(非缺口,查一次即可):事件导致的内存泄漏 / lapsed listener(失效监听器)。Unity 语境:MonoBehaviour 被 Destroy 但长命事件仍订阅 → 托管对象不回收 → 触发时访问残废状态。对应铁律:`OnEnable` 里 `+=` / `OnDisable`/`OnDestroy` 里 `-=` 成对。

**Claude 自身错误记录(入复盘,非用户错点):**

- 我在审代码时断言 `testEvent = PrintTxt;` 在声明类 `Start()` 内"编译不过、报 CS0070",**错误**。正确:CS0070 仅拦**外部类**对事件的访问;**声明类内部**的字段式事件,该标识符即编译器生成的私有委托字段,`=` 合法、可调用。
- 性质:单点武断 + 未守修正 D(凭单一规则记忆下死结论,没给"内部/外部访问不同""字段式事件"留可能性)。用户用实测(贴代码 + 坚持"没报错、打印了 A")纠正,是成功信号。
- 教训:诊断门槛(修正 D)"多元归因也应用到你自己的分析上"——我该把它用在自己身上。断言语言层"编译报错"这类硬结论前,先过一遍作用域 / 特例。

**知识点：协程 —— 理解层判定：结构在，机制细节松。**
- 核心机制“谁挂起谁”初答搞反(说成“挂起主线程跑协程”)，纠正后能自己讲对并主动加推论
  (“yield 在死循环里协程就不结束”)。属机制理解偏差、一纠即通，非塌方。
- IEnumerator 名词层“忘了”，声明/开启协程需翻笔记(低频语法，非缺口)。
- yield return 值的类型分派机制：忘(笔记有)，补上。0 被当 null 处理这点实测正确。
- 坑：内存泄漏归错门(误以为协程会泄漏，实为静默停止、方向相反)——已纠正并入清单。
  StopCoroutine 配对、重计算卡帧能接。
- 模式与委托事件段、与一轮完全一致：生疏 + 机制/边界细节松，非真缺口。

**知识点：值类型 vs 引用类型 —— 理解层判定：概念通，存储/传参细节经点拨即通。**
- 赋值/传参“赋数据 vs 赋地址”全对；引用连带变、传参不影响外部等结论对。
- “值类型=栈”不准确说法出现(上一轮也猜过栈)——澄清为“位置由寄居上下文定，对象字段/数组元素在堆”。
- 期间有一段“房子”命名与 Claude 不一致导致的对不上：用户的图本来正确，Claude 一度误读用户语义、基于误读揪矛盾——【Claude 误读，非用户错】，已撤回并统一命名(格子/对象)。
- 传参“坑”：初答前提搞反(以为传 a 格子本身)，纠正为“传副本格子”后立即吃进、能自主复述 x=new 不影响 a。
- 主动追问三次(每 new 是否开新格子 / x 生命周期 / 是否自动释放)刨地基——本段质量高于前两点。
- 模式同前：结构在，细节经点拨即通，非塌方。

**Claude 自身失误记录（入复盘，非用户错）：**
- 误读用户“房子”语义(用户指栈上变量格子，Claude default 当成堆对象)，基于误读连揪两条“矛盾”，用户要求撤回。
- 教训：用户用自定义类比/命名时，先确认其词映射再评判，勿用自己的默认语义套。呼应修正E“别把确认/表述读成挑战”的近亲——别把“命名不同”读成“理解错误”。

**知识点：装箱与拆箱 —— 理解层判定：结构在，细节松。**
- 概念+代码例对；拆箱必须显式强转这点代码写对。
- 缺“拷贝、两份独立”这层(初答只说“堆开格子存值”，没点出是拷贝、栈堆两份独立)——补上，实测题确认。
- 性能归因从“转换动作”修正为“堆分配+GC”，并点出装箱=隐蔽堆分配。
- 泛型↔装箱连接初答缺失(“想不到例子”)，搭梯(ArrayList vs List\<int>)后立即接住并能自主复述。
- 模式同前：生疏+细节松，非塌方。

**Claude 自身失误记录（入复盘，非用户错）：**
- 本段中途我基于用户【未提出】的一个问题(o 是否万能)自行展开，并替用户回答了本应他答的验证题(违反规则8越俎代庖)，还误触一次 bash 工具。用户指出后全部撤回。
- 教训：
	- ①严守“先逼他答再给答案”，不替他答验证题；
	- ②不基于用户没问的问题展开；
	- ③收到用户答复前先判断“这一轮该我说话还是等他”，别抢拍。呼应规则7(闭卷优先)、规则8(不越俎代庖)。

**知识点：常用容器 List/Dictionary —— 理解层判定：生疏为主 + 一处概念混淆(已澄清)。**
- 底层结构/扩容：闭卷几乎遗忘，一看即回(用户自评“看一遍就都回忆起来”)。数组/翻倍/哈希表/哈希映射均属生疏非缺口。扩容“新数组+全量拷贝”补上。
- 一处概念混淆(值得记)：把“按下标访问”与“按值查找”混为一谈 → 当时误以为 list\[5] 是 O(n) 要遍历(实为 O(1) 直达)。纠正后能自主复述正确成本地图。属标签混淆、一提即回，与前五点同型。
- 选择依据补正：List 中间/开头增删 O(n)，非强项；Dictionary 按 key 增删 O(1)。
- 与前五点对比：同型(结构在、生疏)，不单列为“真误区”。

**Claude 自身失误记录（入复盘，非用户错）：**
- 出题“末尾Add vs 下标0插入谁更耗时”未写“假设不扩容”。用户分情况回答(容量未满/已满)属【审题周全】——把扩容这个客观变量纳入是对的。Claude 预设唯一标准答案(前插O(n))、据此误判用户“回避/绕路”，经用户指出后全部撤回，相关档案A追加项一并撤销。
- 教训：
	- ①比较两操作成本类问题，出题时先钉死无关变量(如扩容)再问；
	- ②别预设唯一标准答案——用户给出技术正确且更周全的回答时，先认其正确，勿套“绕路”帽子。修正D(多元归因也用于自身分析)本轮第三次失守(前两次：内部=判断、误读“房子”语义)。

**知识点：类型转换 is/as/强转 —— 理解层判定：生疏 + 边界细节松，非塌方。**
- is 会；as 初答“不记得”，但给机制 + 测试题(string as Player → null)答对，属遗忘非空缺(若从没建立会猜“崩”)。
- 核心考点“as失败给null vs 强转失败抛InvalidCastException”初答答不出，补上后能用。
- 强转会用但边界表述模糊；模式匹配 is Type 变量 未主动用(第4问写成 is 判断+as 转换两步且结果没接住)——给了即懂。
- Claude 一度猜“as 可能是真空缺”，单题验证为生疏，已收回(修正D：单点不定性)。
- 模式同前。

**知识点：属性 property —— 理解层判定：真生疏 + 直觉底子在，一搭骨架即能接并动手写。**
- 概念/自动属性/只读属性 初答均“忘了”，但第3、4问直觉摸对方向(“安全”“可读不可写”)——非空缺(空缺是连方向都摸不到)。
- 搭骨架后：只读属性 =>  写法正确；钳制属性主动用表达式体 + 知道要在 set 拦。
- 代码 bug：set 钳制分支里 value/volume 混用(>100分支写对 value=100，<0分支误写 volume=0)，
  且最后 volume=value 会覆盖钳制 → 根子是分支未统一操作 value。属“手上精度”问题，同一轮 fencepost/countX 机械滑点，非不懂原理。
- “要加逻辑故不能用自动属性”——用行动答对(直接写完整版)，理由已确认。
- 模式同前：生疏 + 细节松，非塌方。

**知识点：接口 vs 抽象类 —— 理解层判定：生疏到底但非空缺(遗忘程度最深，一激活即回)。**
- 初答四个“不记得/没概念”，遗忘比其他点深。但搭最小骨架(是什么vs能做什么)后，第2、3问能【双向自主套用】判据(能做什么→接口 / 是什么→抽象类)，非复述。
- 当场分辨结论：生疏(学过忘光、一激活回)，非空缺(空缺则接骨架也推不动方向题)。与本轮其他点同型，仅遗忘更深。
- 三硬区别(有无实现/有无字段/继承数量)、“类常常既继承抽象类又实现多接口”给了即懂。
- 模式同前。

**知识点：class vs struct —— 理解层判定：本轮唯一【当场用三步法完整走完】的点，质量最高。**
- 初始纯再认(用户自评"只记结论struct值类型/class引用类型、推不出区别")。
- 三步法实测有效：第一步用户自造 Player/Position 例子且选得准(有直觉但不自信)；
  第二步经 Claude 递问题、从例子推出【赋值行为/null/默认值/构造约束】四条区别，全是推非背；
  用户两次主动追问("默认值是谁的功劳""struct凭什么靠无参构造")逼出深层机制。
- 末尾用户独立串述基本走通(从被递问题→自己串)，是"被推→自走"的实际位移。
- 残留松点：③"自定义构造必须赋全"的归因，反复在"字段初始化机制失效(错)"↔"语言强制对全字段负责(对)"间滑，
  留给三步法第三步(无提示复述)重点盯。
- 意义：这是三步法(档案B新方法论)的首次实测样板，证明"例子锚定→主动推导"能把用户从纯再认带到自主推导。

**Claude 自身失误记录（入复盘，非用户错）：**
- 上一轮我措辞"struct 靠默认无参构造给字段清零"不严谨，制造了 class(内存清零)/struct(构造清零)的假区别。
  实为两者都靠默认初始化、与构造函数无关。用户追问"struct那边为什么是无参构造在做这件事"逼出此矛盾，已收回。
- 教训：讲"默认值/初始化"这类底层机制，先分清"默认初始化(内存层)"与"构造函数(代码层)"，
  别把前者的功劳安到后者头上。呼应本轮反复教训——下硬结论前过一遍机制层次。

**来源**:实战 · 第二轮通用基础摸底会话

**【档案 D · 场景 02 · 追加(Unity组·第1点)】**

**知识点:MonoBehaviour 生命周期 —— 理解层判定:结构在,机制真懂(非名词记得),质量高于C#组平均。**

- 六回调排序错1个:FixedUpdate 误置末尾,余5个位置+职责全对(Awake最优先/OnEnable多次/Start初始化/Update主逻辑/LateUpdate跟随摄像机避穿模,LateUpdate理由自主讲对)。
- 高质量点:能从"固定步长"自主推出"FixedUpdate 一帧执行0~N次"这一多数人背不出的后果;并主动追问累加器机制,自己把形状推出来(只差准确措辞:说成"执行了物理步长时间的逻辑",纠为"累积真实时间攒够一个步长")。属再认→理解的实际位移。
- GetKeyDown 放位:结论对(放Update)、"高帧fix可能不执行→漏检"对;但把放错的后果说轻为"性能消耗",实为功能失灵(漏读/重读)。已纠,列入Obsidian。
- Awake/Start 跨对象时序:初答"感觉都行"(误区),经讲透"全场Awake先于全场Start、跨对象Awake间无保证"后,换场景(SaveLoader读单例)独立推对,并主动追问延伸边界。位移完成。
- 主动追问三次(累加器机制/一个Start执行是否全体Awake已完成/中途Instantiate是否破坏时序),质量高,均属往边界钻,非虚惊。

**模式:** 与C#组、一轮完全一致——结构在、生疏/边界细节松,非真缺口。本点甚至生疏度低于C#组多数点。

**Claude 自身记录:** 之前"帧率很高就一次都不跑"表述给窄了(只对单个0.008秒的帧成立,非高帧率下fix彻底不跑),用户揪出歧义,已澄清。教训呼应本轮警示——讲机制边界先分清"针对某一帧"还是"针对稳定帧率全局"。

**来源**:实战 · 第二轮通用基础摸底会话(Unity组)

**【档案 D · 场景 02 · 追加(Unity组·第2点)】**

**知识点:Prefab/实例化/Variant —— 理解层判定:核心理解到位,1个真误区(runtime同步)+1个概念混淆(SO)。**

- 动态生成必要性:理解到位("运动伴随新对象生成,不可能提前全摆完")。
- Prefab vs GameObject:用户初表述有歧义("Prefab是提前设好参数的GameObject"),Claude先误判为"图纸/成品混淆"并纠;用户澄清其本意为"实例产物是绑定.prefab的GameObject"——本意正确,Claude收回误判。【又一次Claude误读用户表述,见下自身记录】
- 真误区(非生疏):runtime在代码改Prefab资产,已实例化对象是否更新——用户答"会"(错),实为"不会,已脱钩"。用户能在"编辑器期同步"直觉上推错到runtime,是典型高频坑。经讲透"实例出生即独立、编辑器同步≠runtime成立"后应已纠。列Obsidian。
- 概念混淆:用户以为ScriptableObject能做"加受击闪红"。澄清SO=纯数据不进场景、做不了运行时行为;加行为改Prefab、改数值用SO、二者配合非替代。列Obsidian。
- 实例属性override("1个敌人不跟图纸变"):用户能顺"override"钩子自主推出"不会跟着变",再补机制(实例级override与图纸脱钩、Revert还原)。非缺口,知道即可。
- Variant:新知识,用户没接触过(如实说,未硬编,合格)。用"继承/基类子类/override"锚定讲解,对上C#心智模型。

**模式:** 主体仍是"结构在、生疏",但本点首次出现2个非生疏项(runtime同步误区+SO混淆),性质是"把编辑器直觉误推到runtime""把物体模板与纯数据容器混为一类",非无根缺口,一经锚定即通。区别于弧度/坐标衔接那种需专项重建的硬缺口。

**Claude 自身记录(入复盘,非用户错):**
- 用户表述"Prefab是提前设好参数的GameObject"有歧义,Claude挑"错"的读法(图纸/成品混淆)去纠,未先确认用户词映射。用户澄清本意正确后Claude收回。这是本轮(继C#组多次后)又一次同型失误:把"表述歧义/不同"当"理解错误"。教训:纠错前先确认用户的词到底映射到什么,尤其表述可作多种解读时,先问再判。呼应修正E近亲、修正D(多信号才定性、对自己也适用)。

**来源:** 实战 · 第二轮通用基础摸底会话(Unity组)

**【档案 D · 场景 02 · 追加(Unity组·第3点)】**

**知识点:坐标系与坐标转换(世界/本地/屏幕/视口 + 转换方向) —— 理解层判定:结构在,方向感对,术语两处松,非缺口。**

- 世界/本地概念:方向对(世界=相对世界原点绝对位置;本地=相对父)。措辞松:本地说成"相对父对象中心点",经设"父无旋转无缩放"场景验证(Child localPosition(3,0,0)+Parent世界(10,0,0)→算出世界(13,0,0)),判定核心理解在,"中心点"属随口用词非理解错。钉准:本地=相对父Transform(位置+旋转+缩放)的偏移,父转/缩子跟随。
- 屏幕坐标:全对(左下原点/像素/分辨率为界)。补右上=(Screen.width,Screen.height)。
- 视口坐标:用户未主动提,补上(0~1归一化、不依赖分辨率)。知道即可。
- 转换方向感(重头):
	- 场景A(UI血条跟随3D玩家):用户答"世界转视口",方向对(世界往屏幕侧转),终点差一格——视口是0~1比例、摆UI还差换算,正解=世界转屏幕WorldToScreenPoint拿像素。非错,刻度版选岔。
	- 场景B(鼠标点选3D物体):用户主动把射线检测机制完整讲出("鼠标发射线,碰碰撞器就操作")——加分,说明做项目真撞过(小车有射线检测)。但坐标术语错:说"屏幕转本地坐标",本地是父子相对、与屏幕射世界无关。正解=屏幕坐标→ScreenPointToRay生成世界空间射线→Raycast检测。列Obsidian。
- 真错点(非生疏):B"屏幕坐标转本地坐标"——本地/世界术语未分家,顺手抓了"本地"。列Obsidian。

**模式:** 与Unity前两点、C#组、一轮一致——结构在、生疏、术语松,非硬缺口。方向感底子在(A/B大方向均对),卡在坐标系术语精确度(视口vs屏幕刻度、本地vs世界)。区别于弧度/坐标衔接那种需专项重建的硬缺口。

**来源:** 实战 · 第二轮通用基础摸底会话(Unity组)