## 移除对象
```csharp
GameObject.Destroy(Object对象变量, 时间);
```
## 实例化对象
```csharp
//[[12 GameObject]]
GameObject.Instantiate(Object对象变量);
GameObject.CreatePrimitive(PrimitiveType.Cube);
```
## 音量、静音

`audioSource.mute`
`audioSource.volume`

## 判断对象的标签

```csharp
gameObject.CompareTag("Tag");
```

## 计算两点之间的距离

```csharp
Vector3.Distance(v1, v2)  
```

## 世界坐标转屏幕坐标

```csharp
Camera.main.WorldToScreenPoint(Vector3)
```

## 切换场景

```csharp
SceneManager.LoadScene("场景名");
SceneManager.GetActiveScene().name //当前场景
```

## 时间停止

```csharp
Time.timeScale = 0;
```

## 泛型约束

```csharp
//[[06 泛型约束]]
where T : class
```

## 失活激活

```csharp
//[[11 MonoBehavior 重要内容#^c8e3b2]]
this.enabled = false;//失活
this.enabled = true;//激活
//[[12 GameObject#^bcb49e]]
gameObject.SetActive(false);//失活
gameObject.SetActive(true);//激活
```

## 切换场景

```csharp
//[[22 场景切换]]
SceneManager.LoadScene("场景名");
```

## 退出游戏

```csharp
//[[24 补充内容]]
Application.Quit();
```

## is 和 as

```csharp
//[[15 里氏替换原则与is as]]
```

## List.Sort()

```csharp
//[[16 List 排序#^ba42bd]]
list.Sort((a, b) => a > b ? 1 : -1);
```

## 检测鼠标输入

```csharp
//[[15 Input 输入]]
 //按AD返回 -1到1 之间的 左右
Input.GetAxis("Horizontal");
//按WS返回 -1到1 之间的 下上
Input.GetAxis("Vertical");
//鼠标横向移动返回 -1到1 左右
Input.GetAxis("Mouse X");
//鼠标竖向移动返回 -1到1 下上
Input.GetAxis("Mouse Y");
//鼠标按下一瞬间 进入一次  0左键 1右键 2中键  返回bool
if (Input.GetMouseButtonDown(0))
// 鼠标滚轮
Vector2 scroll = Input.mouseScrollDelta;  // ​y = -1 往下滚 y=0 没有滚 y= 1往上滚
float ms = Input.GetAxis("Mouse ScrollWheel"); // 鼠标中键滚动
```

## 实例化并设为子对象

```csharp
GameObject weaponObj = Instantiate(weapon, weaponPos, false);
```

## 两点距离

```csharp
Vector3.Distance(transform.position, targetPos.position)
```

## 拓展方法

```csharp
//[[11 拓展方法]]

// 语法：访问修饰符 static 返回值 函数名(this 拓展类名 参数名, 其他参数...)
static class ExtensionMethods
{
	public static int Square(this int i)
	{
		return i * i;
	}
}
```

## 钳制函数

```csharp
//[[01 数学计算公共类Mathf#^8a75be]]
Mathf.Clamp(10, 11, 20) // 11
```

## 生命周期函数

```csharp
//[[09 脚本生命周期]]
```

## 屏幕坐标转世界坐标

```csharp
//[[03 Unity中的坐标系]]
Camera.main.ScreenToWorldPoint
```

## 类型转换

```csharp
//[[09 类型转换]]
//语法 变量类型.parse("字符串")
int i = int.Parse("123");
float f = float.Parse("3.14");
bool b = bool.Parse("True"); // 必须是 "True" 或 "False"
char c = char.Parse("A"); // 只能是单个字符
//配合异常捕获

try
{
	int i = int.Parse(Console.WriteLine());
}
catch { }

```

## 让自定义类显示在Inspector窗口

```csharp
[System.Serializable]
```

## dictionary

```csharp
//[[08 Dictionary]]
// 遍历所有键
foreach (int key in dic.Keys)
    Console.WriteLine(key + " : " + dic[key]);

// 遍历所有值
foreach (string val in dic.Values)
    Console.WriteLine(val);

// 键值对一起遍历（变量类型用 KeyValuePair 结构体）
foreach (KeyValuePair<int, string> kv in dic)
    Console.WriteLine(kv.Key + " : " + kv.Value);
```

## 反射

```csharp
Activator.CreateInstance(type);//通过反射new一个新对象
```


## 延迟函数

```csharp
//[[06 延迟（延时）函数]]
Invoke("函数名", 延迟时间);
```



# 踩坑记录

## Input.mousePosition 和 Camera.main.ScreenToWorldPoint （坐标转换）

如果直接在`Camera.main.ScreenToWorldPoint()` 中传入`Input.MousePos ` 此时转换过来的世界坐标是基于`Input.MousePos `的坐标计算的，`Input.MousePos ` 的z默认是0，所以计算之后的结果永远是靠近摄像机视口的那个点，所以要先记录`Input.MousePos `得到的Vector3，改变z值，再转换成屏幕坐标